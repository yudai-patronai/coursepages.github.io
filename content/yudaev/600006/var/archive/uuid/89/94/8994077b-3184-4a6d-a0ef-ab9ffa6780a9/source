#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[], char* envp[])
{
    int i;
    for(i = 0; i < argc; i++)
    {
        printf("%s", envp[i]);
    }
    return 0;
}
