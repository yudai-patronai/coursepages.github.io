#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int n, char** a, char** b)
{
    int i;
    int j;
    for (j = 0; j < n; j++)
    {
        int s = atoi(a[1]);
        for (i = 0; i < s; i++)
        {
            printf("%s", b[i]);
        }
    }
    return 0;
}