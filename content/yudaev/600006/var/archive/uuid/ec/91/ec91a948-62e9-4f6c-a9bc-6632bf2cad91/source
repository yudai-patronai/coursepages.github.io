#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[], char *envp[])
{
    int i, n = 0;
    if (1 < argc)
        sscanf(argv[1], "%d", &n);
    for (i = 0; i < n; i++)
    {
        printf("%s\n", envp[i]);
    }
    return 0;
}
