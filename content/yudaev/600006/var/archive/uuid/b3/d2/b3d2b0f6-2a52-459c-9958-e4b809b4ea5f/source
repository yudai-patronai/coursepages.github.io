#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdio.h>

int main(int n, char** a)
{
    int i;
    if (n != 1)
        {
            for (i = 0; i < n; i++)
            {
                printf("%s ", a[i]);
            }
        }
    else
        return 0;
}
