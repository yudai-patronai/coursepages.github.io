#include<stdio.h>
int main(int arg, char *argv[], char *envp[])
{
	int i, n;
	sscanf( argv[1], "%d", &n);
	for (i = 0; (i < n) && envp[i] ; ++i)
	{
		printf("%s\n", envp[i]);
	}
	return 0;
}
