#include <stdio.h>

int main(int argc, char* argv[], char *envp[])
{
	int i, n;
	n = sscanf(argv[1], "%d", &n);
	for (i = 1; (i <= n) && (envp[i]); i++)
		printf("%s\n", envp[i]);
	return 0;
}