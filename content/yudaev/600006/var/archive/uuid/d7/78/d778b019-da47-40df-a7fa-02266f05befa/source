#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char * argv[], char * envp[])
{	
	int i, n;
	sscanf(argv[1], "%d", &n);
	for (i = 0; i < n && envp[i]; i++)
		{
			printf("%s\n", envp[i]);
		}
	return 0;
}