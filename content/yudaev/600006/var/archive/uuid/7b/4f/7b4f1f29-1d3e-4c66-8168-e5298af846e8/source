#include <stdio.h>
int main (int argc, char* avgc[])
{
    int i , a = 0  , b = 0 ;
    for(i = 1; i < argc+1 ; i++)
    {
        sscanf(avgc[i] , "%d" , &b);
        a = a + b ; 
    }
    return a;
}