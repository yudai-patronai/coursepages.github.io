#include<stdio.h>
int main (int argc , char* argv[], char* envp[])
{
    int a , i;
    sscanf(argv[1], "%d" , &a);
    for(i = 0 ; i < a ; i ++)
        printf("%s\n", envp[i]);
    return 0;
}