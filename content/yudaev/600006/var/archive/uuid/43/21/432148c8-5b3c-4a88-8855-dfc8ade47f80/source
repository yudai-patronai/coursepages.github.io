#include <stdio.h>

int main(int argc, char* argv[], char* envp[])
{

    int res = 0;
    int i = 0;
    int num;
    if(argc > 1)
    {

        sscanf(argv[1], "%d", &num);

        //num = 5;

        for(i = 0; i < num && envp[i]; ++i)
        {

            printf("%s\n", envp[i]);

        }

    }

    return 0;

}

