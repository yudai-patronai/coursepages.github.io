#include <stdio.h>

int main(int argc, char *argv[])
{
    int i, sum, k;
    sum = 0;
    for(i = 1; i < argc; i++)
    {
        sscanf(argv[i], "%d", &k);
        sum =  sum + k;
    }
    return sum;
}
