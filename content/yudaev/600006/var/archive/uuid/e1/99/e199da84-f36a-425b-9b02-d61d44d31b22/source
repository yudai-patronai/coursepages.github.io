#include <stdio.h>

int main(int argc, char * argv[])
{
    int i, n, k;
    k = 0;
    for (i = 1; i <  argc; i++)
    {
        sscanf(argv[i], "%d", &n);
        k += n;
    }

    return k;
}
