#include <stdio.h>
#include <math.h>

int FileSize()
{

    int res = 0;
    char c = '\0';

        while((c = getc(stdin)) != EOF)
    {

        res++;

    }

    return res;

}

int main(int argc, char* argv[])
{

    if(freopen("output.txt", "w", stdout) == NULL)
    {

        printf("Usage: stat filename");
        return 1;

    }

    //    #define argc 2
    //    char* arr[2] = {"", "input.txt"};
    //    #define argv arr

    if(argc < 2)
    {

        printf("Can't open file %s", argv[1]);
        return 2;

    }

    if(freopen(argv[1], "r", stdin) == NULL)
        return 100;


    printf("%d", FileSize());


    fclose(stdin);
    fclose(stdout);
    return 0;

}
