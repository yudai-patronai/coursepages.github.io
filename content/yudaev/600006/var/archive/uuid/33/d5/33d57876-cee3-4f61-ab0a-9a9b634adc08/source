#include <stdlib.h>
#include <stdio.h>

int main(int argc, const char** argv)
{
	if (argc == 1)
		return 0;
	int i;
	int result = 0;
	for (i = 1; i <= argc; i++)
		result += atoi(*(argv + i));
	printf("%d\n", result);
	return result;
}