<?xml version="1.0" encoding="utf-8" ?>
<!-- $Id$ -->
<problem
	package = "ru.fipi.math.2009.demo"
	id = "argv1">
	<statement language="ru_RU">
		<title>Аргументы командной строки</title>
		<description>
<p>Вычислить сумму нескольких целых чисел, заданных в <a href="http://acm.mipt.ru/twiki/bin/view/Cintro/UsingArgv">аргументах командной строки</a>.</p>

<p>Стандартный поток ввода (stdin) не используется.</p>
<p>Стандартный поток вывода (stdout) не используется.</p>
<p>Аргументы в программе либо отсутствуют (тогда суммой считать 0), либо целые числа.
</p>
<p>Сумму вернуть в коде возврата программы.
</p>

<p>Примеры:
</p>

<table>
<tr>
    <td>
        <b>Параметры программы</b>
    </td><td>
        <b>Код возврата</b>
    </td>
</tr>
<tr>
    <td>3 15 9 20
    </td><td>47
    </td>
</tr>
<tr>
    <td>12 21 -11 44
    </td><td>66
    </td>
</tr>
<tr>
    <td>
    </td><td>0
    </td>
</tr>

</table>

<hr/>        
<p>Для преобразования строки в число используйте стандартную функцию языка С <a href="http://www.opennet.ru/man.shtml?topic=strtol">strtol</a>
</p>  

<pre>
#include &lt;stdlib.h&gt;

long int strtol(const char *nptr, char **endptr, int base);
</pre>      
        
<p>Функция strtol() конвертирует начальную часть строки nptr в длинное целое в соответствии с указанным base, которое должно находиться в диапазоне от 2-х до 36-х включительно или быть равным нулю.
</p>        
        
<p>Строка должна начинаться с пустых символов (определяемых при помощи isspace(3)), далее должен следовать один знак: `+' или `-' (если есть). Если base равно нулю или 16-и, то строка может начинаться с приставки `0x', что означает использование шестнадцатеричной системы исчисления; иначе, если base равно нулю, используется десятичная система исчисления, кроме случая, когда последующий символ также равен `0': тогда используется восьмеричная система исчисления.
</p> 

<p>Остаток строки, таким образом, конвертируется в целое число (long int). Этот процесс останавливается, если в строке встречается некорректное значение (например, не соответствующая системе исчисления цифра). Символ `A' в верхнем или нижнем регистре означает 10, `B' означает 11 и так далее (до `Z', означающей 35).
</p>       
<p>Если значение endptr - не NULL, то strtol() записывает адрес первого некорректного значения в *endptr. Если в строке вообще нет цифр, то strtol() сохраняет начальное значение nptr в *endptr (и возвращает 0). В частности, если *nptr не равно '\0', а **endptr равно '\0' по возвращении, то вся строка состоит из корректных символов.
</p>       
<p>Функция strtoll() работает так же, как и strtol(), но возвращает значение типа long long int.
</p>

<p>Функция strtol() возвращает результат преобразования, если значение не вызвало "переполнения" или не было крайне маленьким. Если происходит последнее, то strtol() возвращает LONG_MIN. Если же произошло "переполнение", то strtol() возвращает LONG_MAX. В обоих случаях глобальной переменной errno присваивается значение ERANGE. То же самое относится к функции strtoll() (только вместо LONG_MIN и LONG_MAX возвращаются значения LLONG_MIN и LLONG_MAX).  
</p>  
     
<p>Для конвертации строки в дробные числа используйте функции :
</p> 
<pre>
double strtod(const char *nptr, char **endptr); 
float strtof(const char *nptr, char **endptr); 
long double strtold(const char *nptr, char **endptr);  
</pre>
		</description>
        <notes>Авторы: Третьяков Андрей, Дербышева Т.Н.
        </notes>
	</statement>
</problem>
