n, m=map(int,(input().split()))
a=[list(map(int, input().split())) for p in range(n)]

def Transpose(A):
    n=len(A)
    m=len(A[0])
    a=[[A[i][p]for i in range(n)]for p in range(m)]


    return a
a=Transpose(a)
for row in a:
    print(' '.join([str(elem) for elem in row]))