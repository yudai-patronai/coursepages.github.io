#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    int a, b, c, i, j;
    scanf("%d", &n);
    int** p = (int **) malloc(n * sizeof(int*));
        for (i = 0; i < n; i++)
            p[i] = (int*)malloc(3 * sizeof(int));
    for (i = 0; i < n; i++)
    {
        for (j = 0; j < 3; j++)
            scanf("%d", &p[i][j]);
    }
    i = 0; j = 0;
    a = p[0][0];
    b = p[0][1];
    c = p[0][2];
    for (i = 0; i < n; i++)
    {
        for (j = 0; j < 3; j++)
        {
            if (i < (n - 1))
            {
                p[i + 1][j] = p[i + 1][j] + p[i][0];
            }
            if (i < (n - 2))
            {
                p[i + 2][j] = p[i + 2][j] + p[i][1];
            }
            if (i < (n - 3))
            {
                p[i + 3][j] = p[i + 3][j] + p[i][2];
            }
        }
    }
    free(p);
    return 0;
    11
}