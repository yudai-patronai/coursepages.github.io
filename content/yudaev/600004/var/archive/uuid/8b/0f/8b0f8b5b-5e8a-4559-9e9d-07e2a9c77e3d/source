#include <stdio.h>

/*
int r (int x, int y, long long int **N)
{
	int i=0;
	long long int s=0;
	if (x<=y) return N[x][x];
	else
	{
		if (y==1) return 1;
		else
		{
			for (i=y;i>0;i--)
			{
				s=s+N[x-i][i];
			}
			N[x][y]=s;
			return s;
		}
	}
}
*/
int main()
{
	long long int N[101][101];
	int i=0, j=0;
	for (i=0;i<101;i++) for(j=0;j<101;j++) N[i][j]=1;
	int n=0;
	scanf("%d", &n);
	for (i=2;i<=n;i++)
	{
		for (j=2;j<i;j++)
		{
			if (i-j>=j) N[i][j]=N[i][j-1]+N[i-j][j];
			else N[i][j] = N[i][j-1]+(i-j);
		}
		N[i][i]=1+N[i][i-1];
	}
	printf("%lld", N[n][n]);
	return 0;
}
