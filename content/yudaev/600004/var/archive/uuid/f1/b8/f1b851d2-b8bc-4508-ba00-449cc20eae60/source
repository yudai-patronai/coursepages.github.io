#include <stdio.h>
#include <stdlib.h>

int main()
{
    int m1, n1, m2, n2;
    int i, j, k;
    int c = 0;
    scanf("%d %d %d %d", &n1, &m1, &n2, &m2);
    if (m1 != n2)
        printf("-1");
    else
    {
        int p[100][100];
        int q[100][100];
        //int** p = (int **)malloc(n1 * sizeof(int*));
        //for (i = 0; i < n1; i++)
        //
        //    p[i] = (int* )malloc(m1 * sizeof(int));
        //}
        //int** q = (int **)malloc(n2 * sizeof(int*));
        //for (i = 0; i < n2; i++)
        //{
        //    q[i] = (int* )malloc(m2 * sizeof(int));
        //}
        for (i = 0; i < n1; i++)
            for (j = 0; j < m1; j++)
                scanf("%d", &p[i][j]);
        for (i = 0; i < n2; i++)
            for (j = 0; j < m2; j++)
                scanf("%d", &q[i][j]);

        for (i = 0; i < n1; i++)
        {
            for (j = 0; j < m2; j++)
            {
                for (k = 0; k < m1; k++)
                {
                    c = c + p[i][k]*q[k][j];
                }
                printf("%d ", c);
                c = 0;
            }
            printf("\n");
        }
        //free(p);
        //free(q);
    }
    return 0;
}
