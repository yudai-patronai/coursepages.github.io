#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    int a, b, c, i, j;
    scanf("%d", &n);
    int p[1000][4];
 //   int** p = (int **) malloc(n * sizeof(int*));
  //     for (i = 0; i < n; i++)
   //        p[i] = (int*)malloc(3 * sizeof(int));
    for (i = 0; i < n; i++)
        for (j = 0; j < 3; j++)
             p[i][j] = 0;
    for (i = 0; i < n; i++)
    {
        for (j = 0; j < 3; j++)
        {
            scanf("%d", &p[i][j]);
        }
    }
    i = 0; j = 0;
    a = 0;
    b = 0;
    c = 0;
    for (i = 0; i < n; i++)
    {
        for (j = 0; j < 3; j++)
        {
            if (i < (n - 1))
            {
                p[i + 1][j] = p[i + 1][j] + p[i][0] - b;
            }
            if (i < (n - 2))
            {
                p[i + 2][j] = p[i + 2][j] + p[i][1] - c;
            }
            if (i < (n - 3))
            {
                p[i + 3][j] = p[i + 3][j] + p[i][2];
            }
        }
        b = p[i][1];
        c = p[i][2];
//       for(j = 0; j < n; j++)
 //          printf("%d %d %d\n", p[j][0], p[j][1], p[j][2]);
 //       printf("\n");
    }
    if (n >= 3)
        if (p[n-1][0] <= p[n-2][1])
            if (p[n-1][0] <= p[n-3][2])
                printf("%d", p[n - 1][0]);
            else 
                if (p[n-3][2] <= p[n - 2][1])
                   printf("%d", p[n - 3][2]);
                else 
                   printf("%d", p[n - 2][1]);
        else 
            if (p[n-2][1] <= p[n-3][2])
                printf("%d", p[n - 2][1]);
            else 
                if (p[n-3][2] <= p[n - 2][1])
                   printf("%d", p[n - 3][2]);
            else
                printf("%d", p[n-3][2]);
    else
        if (n == 2)  
           if (p[n-1][0] <= p[n-2][1])
              printf("%d", p[n-1][0]);
           else 
               printf("%d", p[n-2][1]);
        else 
            printf("%d", p[n-1][0]);
   // free(p);
    return 0;
}