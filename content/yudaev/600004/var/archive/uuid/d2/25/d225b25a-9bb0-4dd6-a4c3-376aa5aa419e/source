#include <stdio.h>
#include <stdlib.h>

int fact(int n)
{
    int f = 1;

    while ( n > 1)
    {
        f*=n;
        n--;
    }
    return f;
}

int main()
{
    int n,m;

    scanf("%d%d", &n, &m);

    printf("%d\n", fact(n+m)/(fact(n)*fact(m)));
    return 0;
}
