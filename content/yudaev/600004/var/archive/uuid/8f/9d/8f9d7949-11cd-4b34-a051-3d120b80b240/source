// ways.cpp: определяет точку входа для консольного приложения.
//

#include <stdio.h>

int main()
{
	int board[21][21], i, j, n, m;
	scanf("%d %d", &n, &m);
	board[0][0] = 1;
	for (i = 0; i < n; i++)
		for (j = 0; j < m; j++)
			if (i || j)
				if (j && i)
					board[i][j] = board[i - 1][j] + board[i][j - 1];
				else
					if (i)
						board[i][j] = p[i - 1][j];
					else
						board[i][j] = p[i][j - 1];

	printf("%d", board[n - 1][m - 1]);

    return 0;
}

