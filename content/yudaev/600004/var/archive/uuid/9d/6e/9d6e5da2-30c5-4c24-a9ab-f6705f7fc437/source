#include <stdio.h>
#include <stdlib.h>

int g, e, f;

int min (int a, int b, int c)
{

        if (a <= b)
            if (a <= c)
                return a;
            else
                if (c <= b)
                   return c;
                else
                   return b;
        else
            if (b <= c)
                return b;
            else
                if (c <= a)
                   return c;
            else
                return a;
}

int main()
{
    int n;
    int a, b, c, i, j;
    scanf("%d", &n);
    int p[100000][4];
    for (i = 0; i < n; i++)
        for (j = 0; j < 3; j++)
             p[i][j] = 0;
    for (i = 0; i < n; i++)
    {
        for (j = 0; j < 3; j++)
        {
            scanf("%d", &p[i][j]);
        }
    }


    if (n >= 3)
    {
        a = a[n-1][0];
        b = a[n-2][1];
        c = a[n-3][2];
    }
    else
        if (n == 2)
        {
            a = a[n-1][0];
            b = a[n-2][1];
        }
        else
            a = a[n-1][0];


    for (i = n-1; i <> 0; i--)
    {

//       for(j = 0; j < n; j++)
 //          printf("%d %d %d\n", p[j][0], p[j][1], p[j][2]);
 //       printf("\n");
    }

    return 0;
    11
}
